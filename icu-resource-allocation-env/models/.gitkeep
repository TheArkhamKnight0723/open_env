# This directory holds the trained PPO model (ppo_icu_agent.zip).
# The model file is generated by running:
#
#     python -m training.train_ppo
#
# It is excluded from version control via .gitignore because the zip
# can be several hundred MB. If you want to commit a lightweight checkpoint,
# run training with --timesteps 10000 and commit the resulting zip.
